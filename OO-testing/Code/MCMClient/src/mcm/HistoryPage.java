package mcm;

import java.awt.*;
import java.awt.event.*;
import javax.swing.*;
import javax.swing.event.*;
import javax.swing.border.*;

public class HistoryPage extends JPanel
{
	public MCMClient frame;

	public HistoryPage(MCMClient frame, MainPage mainPage) {
/*		this.frame = frame;

		setLayout(new BorderLayout());

		JList historyList = new JList();
		mainPage.lookAfter(historyList);
		add(new JScrollPane(historyList), BorderLayout.CENTER);
*/	}
}
